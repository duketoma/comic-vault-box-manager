# PostgreSQL connection string for your local database.
DATABASE_URL=postgres://postgres:postgres@localhost:5432/comic_vault

# Optional: set to true when connecting with a local TLS-enabled PostgreSQL server.
# DATABASE_SSL=true

# Required only for the cover-scanning feature.
GEMINI_API_KEY=
